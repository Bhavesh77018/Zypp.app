// We unify the ESG Impact and Environment pages.
// Both /esg-impact and /environment will serve the exact same premium React component.
import EnvironmentPage from "../environment/page";

export default function ESGImpactPage() {
  return <EnvironmentPage />;
}
